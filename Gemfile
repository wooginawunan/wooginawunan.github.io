source 'https://rubygems.org'

# Use GitHub Pages gem for exact compatibility
gem 'github-pages', group: :jekyll_plugins

# Development gems
gem 'webrick' # Required for Ruby 3.0+

group :jekyll_plugins do
  gem 'jekyll-sitemap'
  gem 'jekyll-feed'
end
